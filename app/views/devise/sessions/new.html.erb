<h2>ろぐいんがめんだよ～</h2>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
<div class="ui stacked segment">
  <div class="field">
    <div class="ui left icon input">
      <i class="user icon"></i>
        <%= f.text_field :username, autofocus: true, placeholder: "ユーザ名" %>
    </div>
  </div>
  <div class="field">
    <div class="ui left icon input">
      <i class="lock icon"></i>
      <%= f.password_field :password, autocomplete: "current-password", placeholder: "パスワード" %>
    </div>
  </div>
  <%# <% if devise_mapping.rememberable? %>
    <%# <div class="field">
      <%= f.check_box :remember_me %>
      <%# <%= f.label : %>
    <%# </div>
  <% end %>

  <div class="submit">
    <input type="submit" value="ログイン" data-disable-with="ログイン中" class="ui large teal submit button" />
  </div><br>
<% end %>
  <div class="submit">
      <%= button_to "新規登録", {controller: 'registrations', action: 'new'}, {method: :get} %>
  </div>
</div>